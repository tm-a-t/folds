[project]
name = "comment-enforcer"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "folds",
    "python-dotenv>=1.0.1",
]

[project.scripts]
comment-enforcer = "comment_enforcer:__main__"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
